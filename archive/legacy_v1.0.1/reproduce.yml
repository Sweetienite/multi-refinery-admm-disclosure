name: reproduce

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

jobs:
  reproduce:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Syntax check
        run: python -m py_compile src/*.py scripts/*.py tests/*.py

      - name: Unit tests
        run: python -m pytest tests/ -v --tb=short

      - name: Verify reported results
        if: ${{ always() }}
        run: python scripts/verify_reported_results.py

      - name: Verify final-figure frozen inputs
        if: ${{ always() }}
        run: python figures_final_release/source/validate_data.py

      - name: Audit published tables and final figures
        if: ${{ always() }}
        run: python scripts/validate_public_release.py
