# 公开复现范围

本仓库是当前论文的读者材料包，而非封闭权威证据的镜像。公开部分包括参数合同、汇总结果、表4/表5、图表数据、出图源码、依赖说明和校验器；不包括 raw/private trace、冻结运行时证据、内部 solver forensic、编辑稿、批注、私有仓库地址或访问方式。

严格验证状态为 `PASS`，总体状态为 `PARTIAL_PASS`。前者说明封闭权威源按既定合同通过了严格校验；后者说明两种机制需要不同的锁定运行时，公开读者不应把本仓库理解成任意平台的逐步二进制一致性证明。
