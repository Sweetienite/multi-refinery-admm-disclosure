# 仓库更新内容

1. 用 `code/generate_paper_figures_final.py` 替换或新增论文最终出图入口。
2. 将 `data/` 中5个CSV作为图片权威输入。
3. 将 `figures/` 中6张PNG作为论文定稿图片。
4. 保留残差图中对偶残差为0时的原有断线表达，不使用显示下限替代零值。
5. 仓库文档中的旧图1(a)和旧图3(b)应标记为弃用或移入历史目录。
